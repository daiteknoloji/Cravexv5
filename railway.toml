[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# Healthcheck disabled - Synapse needs time for DB migrations
# Railway will detect port 8008 availability
startCommand = "/bin/bash /start.sh"

